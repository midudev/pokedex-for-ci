#Tiene un nombre
name: Say hello

#Tiene un evento para activarse, se coloca la rama
on:
  push:
    branches:
      - main

#Para trabajar
jobs:
  hello_world:
    runs-on: ubuntu-18.04   #Sistema operativo donde va a funcionar

    #Pasos que va a realizar
    #Debemos manejar bien las tabulaciones
    steps:
      - name: Echo the message
        run: |
          echo "Hola midu!"