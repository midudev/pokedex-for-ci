name: Say Hello

on:
  push:
    branches:
      - main
jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: Hello world action
        run: |
          echo "Hello world!"
      - name: Dime la fecha
        run: date
      - run: ls -l
