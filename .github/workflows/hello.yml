name: Say hello

on:
  push:
    branches:
      - main
jobs:
  hello_world:
    runs-on: ubuntu-18.04
    steps:
      - name: Prueba 1
        run: |
          free -h && nproc
      - name: prueba2
        run: apt update && apt install curl -y && curl ifconfig.me
