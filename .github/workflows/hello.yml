name: Say hello

on:
  push:
    branches:
      - main

jobs:
  hello-world:
    runs-on: ubuntu-18.04
    steps:
      - name: Echo the message
        run: |
          echo "Hola Midu!"
      - name: Dime la fecha
        run: date
      - run: ls -l
          
